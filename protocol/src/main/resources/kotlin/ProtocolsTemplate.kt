${protocol_root_path}
${protocol_imports}
import java.util.*;
public class Protocols {
    ${protocol_class}

    // -----------------------------------------------------------------------------------------------------------------

    ${protocol_registration}
}
