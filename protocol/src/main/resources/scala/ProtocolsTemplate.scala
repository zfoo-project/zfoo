${protocol_root_path}
${protocol_imports}
import scala.collection.mutable

${protocol_class}

// -----------------------------------------------------------------------------------------------------------------

${protocol_registration}
