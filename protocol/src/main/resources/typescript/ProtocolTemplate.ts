{}
{}
class {} {

    {}

    constructor({}) {
        {}
    }

    protocolId(): number {
        return {};
    }

    static write(buffer: Any, packet: {}) {
        {}
    }

    static read(buffer: Any): {} {
        if (!buffer.readBoolean()) {
            return null;
        }
        const packet = new {}();
        {}
        return packet;
    }
}

export default {};
